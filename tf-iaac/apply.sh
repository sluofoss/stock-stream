#TODO take input for 
#   environment
#   steps to continue on
# Steps:
#   tf state_store
#   tf storage (save for artifacts used to create other modules)
#   create artifact
#   tf pipeline (take TF env var that includes sns subscription email (sensitive info not to commit))
